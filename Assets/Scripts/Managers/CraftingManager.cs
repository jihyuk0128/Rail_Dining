using System;
using System.Collections.Generic;

public class CraftingManager
{
    //public List<ItemStack> Slots { get; private set; }
    //public event Action OnCraftingChanged;

    //public CraftingManager(int size)
    //{
    //    Slots = new List<ItemStack>();
    //    for (int i = 0; i < size; i++)
    //        Slots.Add(new ItemStack());
    //}

    //public void SetItem(int index, int id, int amount)
    //{
    //    if (index < 0 || index >= Slots.Count) return;
    //    Slots[index].ItemId = id;
    //    Slots[index].Amount = amount;
    //    OnCraftingChanged?.Invoke();
    //}

    //public void SwapItems(int a, int b)
    //{
    //    if (a < 0 || b < 0 || a >= Slots.Count || b >= Slots.Count) return;
    //    (Slots[a], Slots[b]) = (Slots[b], Slots[a]);
    //    OnCraftingChanged?.Invoke();
    //}
}
